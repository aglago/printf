#ifndef MAIN_H
#define MAIN_H

/* header files */
#include <stdio.h>
#include <stdarg.h>

/* function prototypes */
int _putchar(char c);
int _printf(const char *format, ...);
int _puts(const char *str);

#endif
